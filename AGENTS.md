# Project Instructions

## Package Manager

- This is a standard Nuxt project using `pnpm`.
- Run `pnpm install` after dependency changes.
- Use `pnpm run check` and `pnpm run build` for validation when applicable.
